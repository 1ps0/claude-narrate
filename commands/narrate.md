---
description: Control the narrator — pause, resume, mute, status, or set VOICE/RATE/VOLUME/VERBOSITY/MAX_WAIT
argument-hint: "[pause|resume|mute|status|set KEY VALUE]"
---
Run exactly this and show the output verbatim, nothing else:

```
!"${CLAUDE_PLUGIN_ROOT}/hooks/narrate.sh" $ARGUMENTS
```
